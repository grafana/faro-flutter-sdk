/// Web-safe stub — replaced by [configure_http_overrides_io.dart] on mobile
/// via conditional import in `main.dart`.
void configureHttpOverrides() {
  // No-op on web: FaroHttpOverrides relies on dart:io HttpOverrides.
}
