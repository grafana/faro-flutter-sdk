<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.grafana.faro">
    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="33"
        tools:ignore="GradleOverrides" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application>
        <!-- Samples process importance before Application.onCreate so that
             background process starts are not reported as cold starts.
             See FaroStartupProvider. -->
        <provider
            android:name="com.grafana.faro.FaroStartupProvider"
            android:authorities="${applicationId}.farostartupprovider"
            android:exported="false"
            android:initOrder="200" />
    </application>
</manifest>
